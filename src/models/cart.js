class CartItem {
    constructor(quantity, price, title, sum,image) {
      this.quantity = quantity;
      this.Price = price;
      this.title = title;
      this.sum = sum;
      this.image = image
    }
  }
  
  export default CartItem;